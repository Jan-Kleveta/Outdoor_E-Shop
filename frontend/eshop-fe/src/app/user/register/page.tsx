import RegisterForm from '../../../components/header/RegisterForm';

export default function RegisterPage() {
    return (
        <div className="min-h-screen flex items-center justify-center bg-white text-black font-logo">
            <RegisterForm />
        </div>
    );
}